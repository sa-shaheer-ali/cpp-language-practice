#include <iostream>
#include <algorithm>
bool function(int arr[],int size){
    if(size==1){
        return true;
    }
bool check{
    std::sort(arr[]-1,size-1);
    return true;
}
    return (arr[0]<arr[1]&& check);
}